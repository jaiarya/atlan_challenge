
apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: atlan
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
